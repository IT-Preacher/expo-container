import { ScrollView } from '@/components';
import { Text } from 'react-native';

export default function TabTwoScreen() {
  return (
    <ScrollView>
      <Text>Explore</Text>
    </ScrollView>
  );
}
